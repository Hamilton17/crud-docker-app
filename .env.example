# Configuración de PostgreSQL
POSTGRES_DB=cruddb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Configuración de la Aplicación
DB_HOST=db
DB_PORT=5432
DB_NAME=cruddb
DB_USER=postgres
DB_PASSWORD=postgres

# Puertos de los servicios
APP_PORT=3000
DB_PORT_EXTERNAL=5432

# IMPORTANTE: Este es un archivo de ejemplo.
# Las variables están configuradas directamente en docker-compose.yml
# Si deseas usar un archivo .env, copia este archivo como .env y modifica los valores
